# ReaTrack Backend — залежності
# Встановлення: pip install -r requirements.txt

# ── Web-фреймворк ──────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1

# ── База даних ─────────────────────────────────────────────────────────────
psycopg2-binary==2.9.10

# ── Auth ───────────────────────────────────────────────────────────────────
bcrypt==4.2.1
python-jose[cryptography]==3.3.0

# ── Валідація ──────────────────────────────────────────────────────────────
pydantic==2.10.3

# ── AI / Pose Analysis (для /analyze) ─────────────────────────────────────
# УВАГА: mediapipe тягне opencv-contrib-python (GUI-версія з X11).
# Замість нього використовуємо headless-варіант (без libxcb/libxcb.so.1).
# nixpacks.toml примусово замінює GUI-cv2 на headless після pip install.
mediapipe>=0.10.0
opencv-contrib-python-headless>=4.8.0
numpy>=1.24.0
